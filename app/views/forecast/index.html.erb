<turbo-frame id="forecast">
  <section class="w-full bg-base text-text">
    <div class="relative isolate overflow-hidden pt-16 pb-8">
      <div class="mx-auto max-w-7xl px-6 lg:px-8">
        <div class="mx-auto grid max-w-2xl grid-cols-1 gap-x-8 gap-y-16 lg:max-w-none lg:grid-cols-2">
          <div class="max-w-xl">
            <h2 class="text-3xl font-bold tracking-tight text-text sm:text-4xl">
              Check your local weather
            </h2>
            <div class="mt-8">
              <%# render the weather form %>
            </div>
          </div>
        </div>

        <div class="mt-4 text-love">
          <%# display flash alerts %>
        </div>
      </div>
    </div>

    <%# TODO: Render forecast cards styled with bg-surface, border-overlay, text-subtle %>
  </section>
</turbo-frame>
